import momentFormatParser from 'dummy/utils/moment-format-parser';
import { module, test } from 'qunit';

module('Unit | Utility | moment format parser');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentFormatParser();
  assert.ok(result);
});
